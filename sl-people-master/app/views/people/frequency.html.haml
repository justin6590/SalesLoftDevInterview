%div{style: 'margin-left: 20px;'}
  %h2 Frequency count of all the unique characters in all the email addresses from the people list API, sorted by frequency count.

  %br/

  = link_to 'Back to List of People', people_path, class: 'btn btn-primary'

  %br/

  %table.table.table-hover{style: 'width: 200px; margin-left: 20px;'}
    %thead
      %tr
        %th Character
        %th Count
    %tbody
      - email_frequencies(@people).each do |freq|
        %tr
          %td="#{freq[0]}"
          %td="#{freq[1]}"
